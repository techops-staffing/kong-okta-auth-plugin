-- test fixtures
return {
  encode_base64 = function(str)
  end,
  req = {},
  resp = {},
  now = function()
  end,
  log = function(...)
  end,
  sleep = function(t)
  end,
  timer = {},
  socket = {},
  re = {},
  config = {
    ngx_lua_version = ""
  }
}
